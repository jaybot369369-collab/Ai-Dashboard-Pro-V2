.DS_Store
node_modules/
*.log

.claude/settings.local.json

# Desktop launcher — local path, not for the repo
JayBot.html
JayBot.command

# Morning Brief — PERSONAL (open positions, theses, tendencies). Generated
# locally, served by the local :8768 server. Never commit to this public repo.
js/data/brief.json
